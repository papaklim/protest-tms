server:
  port: 8092

spring:
  application:
    name: protest-cases
  datasource:
    url: jdbc:postgresql://localhost:15432/protest-cases
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
